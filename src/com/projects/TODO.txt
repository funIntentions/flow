Future Thoughts

On Going
- Demand chart brakes if advancing the time by days, should switch between seconds, minutes, hours, ect?
- Cleanup the task manager, remove the testTask from SystemController and have it created in TaskManager if possible
QUESTIONS:

